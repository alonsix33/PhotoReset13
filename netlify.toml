[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: la ruta del panel (/panel) debe resolver a index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
