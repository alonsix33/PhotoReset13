# Placeholders — SIN secretos reales. Cópialos a donde correspondan.
# La clave de impresora y la del panel NUNCA se commitean: van como variables
# de entorno en cada plataforma (Railway / Netlify).

# --- backend (Railway) ---
PRINTER_KEY=cambia-esto
PANEL_PASSWORD=cambia-esto
FRONTEND_ORIGIN=https://tu-sitio.netlify.app
DATA_DIR=/data

# --- frontend (Netlify) — se setea en la UI de Netlify ---
VITE_API_BASE_URL=https://tu-backend.up.railway.app
